# Examples

Add small, reproducible examples as implementations are completed. Keep algorithms
in the package, set local random seeds, document commands, and avoid import-time
training or downloads. Save private data and large outputs outside version control.
